info:
  name: WT-API-604 統計管理
  type: http
  seq: 6

http:
  method: PUT
  url: "{{url}}/batch/statistics"
  body:
    type: json
    data: |-
      {
          "domains": "",
          "target_date_from": "2026-04-07",
          "target_date_to": "2026-04-07"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
