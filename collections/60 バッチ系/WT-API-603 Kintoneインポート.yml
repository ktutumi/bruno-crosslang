info:
  name: WT-API-603 Kintoneインポート
  type: http
  seq: 3

http:
  method: PUT
  url: "{{url}}/batch/kintone-import"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
