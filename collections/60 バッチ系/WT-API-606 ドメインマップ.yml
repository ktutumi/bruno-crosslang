info:
  name: WT-API-606 ドメインマップ
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/batch/contract-info-json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
