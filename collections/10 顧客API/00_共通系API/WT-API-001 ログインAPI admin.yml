info:
  name: WT-API-001 ログインAPI admin
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/login"
  body:
    type: json
    data: |-
      {
        "user_id": "admin",
        "passwd": "12345678",
        "division": "cross",
        "lang": "ja"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
