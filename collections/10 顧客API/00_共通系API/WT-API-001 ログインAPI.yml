info:
  name: WT-API-001 ログインAPI
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/login"
  body:
    type: json
    data: |-
      {
          "user_id": "user24",
          "passwd": "12345678",
          "lang": "ja",
          "division": "customer",
          "protocol": "http"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
