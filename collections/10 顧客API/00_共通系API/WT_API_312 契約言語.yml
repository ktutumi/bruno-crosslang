info:
  name: WT_API_312 契約言語
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/customer/contract-lang"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
