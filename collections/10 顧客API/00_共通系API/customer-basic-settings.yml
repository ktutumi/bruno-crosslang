info:
  name: /customer/basic-settings
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/customer/basic-settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
