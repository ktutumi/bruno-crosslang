info:
  name: Update User
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:5050/api/v1/users/2
  body:
    type: json
    data: |-
      {
        "id": 2,
        "user_id": "user2",
        "password": "secret",
        "first_name": "first",
        "last_name": "last",
        "email": "user2@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
