info:
  name: Get User
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5050/api/v1/users/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
