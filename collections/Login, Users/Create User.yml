info:
  name: Create User
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:5050/api/v1/users
  body:
    type: json
    data: |-
      {
        "user_id": "test_user",
        "password": "secret",
        "first_name": "first",
        "last_name": "last",
        "email": "test_user@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
