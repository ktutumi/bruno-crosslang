info:
  name: GetSystemMessages
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/sitectl/system-messages"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
