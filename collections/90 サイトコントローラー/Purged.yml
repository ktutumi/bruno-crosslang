info:
  name: Purged
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/sitectl/purge/purged"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "ids": [2, 3]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
