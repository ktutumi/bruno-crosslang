info:
  name: "[Local]GetInfo tosei-hotel.co.jp"
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/sitectl?domain=h10-local-transer-test&lang=en"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: domain
      value: h10-local-transer-test
      type: query
    - name: lang
      value: en
      type: query
    - name: service_cd
      value: H20
      type: query
      disabled: true
  auth:
    type: basic
    username: webtranser
    password: webtranser

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
