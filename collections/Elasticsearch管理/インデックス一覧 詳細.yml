info:
  name: インデックス一覧 詳細
  type: http
  seq: 3

http:
  method: GET
  url: "{{es_url}}/_cat/indices?v&s=index"
  params:
    - name: v
      value: ""
      type: query
    - name: s
      value: index
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
