info:
  name: インデックス一覧 ドキュメント数
  type: http
  seq: 2

http:
  method: GET
  url: "{{es_url}}/_cat/indices?h=index,docs.count&s=index"
  params:
    - name: h
      value: index,docs.count
      type: query
    - name: s
      value: index
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
