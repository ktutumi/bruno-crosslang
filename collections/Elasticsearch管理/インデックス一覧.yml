info:
  name: インデックス一覧
  type: http
  seq: 1

http:
  method: GET
  url: "{{es_url}}/_cat/indices?h=index&s=index"
  params:
    - name: h
      value: index
      type: query
    - name: s
      value: index
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
