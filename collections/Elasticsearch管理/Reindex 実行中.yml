info:
  name: Reindex 実行中
  type: http
  seq: 4

http:
  method: GET
  url: "{{es_url}}/_cat/recovery?active_only=true"
  params:
    - name: active_only
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
